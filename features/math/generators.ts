import type { Exercise } from "@/lib/types";
const hints=(a:string,b:string,c:string):[string,string,string]=>[a,b,c];
export function generateAdditionQuestion(difficulty=1,a=7,b=8):Exercise{const scale=10**Math.min(3,difficulty-1);a*=scale;b*=scale;return{id:`add-${a}-${b}`,type:"numeric",prompt:`¿Cuánto es ${a} + ${b}?`,answer:String(a+b),difficulty:Math.min(5,difficulty) as 1|2|3|4|5,hints:hints("Separa decenas y unidades.","Suma cada valor posicional.",`${a} + ${b-1}, y después añade 1.`),explanation:`Al combinar ${a} y ${b} obtenemos ${a+b}.`}}
export function generateFractionComparison(a=2,b=3,c=3,d=5):Exercise{return{id:`frac-${a}${b}${c}${d}`,type:"choice",prompt:`¿Qué fracción es mayor: ${a}/${b} o ${c}/${d}?`,options:[`${a}/${b}`,`${c}/${d}`,"Son iguales"],answer:a/b>c/d?`${a}/${b}`:a/b<c/d?`${c}/${d}`:"Son iguales",difficulty:3,hints:hints("Imagina dos barras iguales.","Busca un denominador común.",`Compara ${a*d} con ${c*b}.`),explanation:`Al multiplicar en cruz comparamos ${a*d} y ${c*b}.`}}
export function generatePercentageProblem(base=100,percent=8):Exercise{return{id:`pct-${base}-${percent}`,type:"numeric",prompt:`Una inversión virtual de $${base} aumenta ${percent}%. ¿Cuánto vale ahora?`,answer:String(base*(1+percent/100)),difficulty:3,hints:hints("Primero calcula el aumento.",`Convierte ${percent}% a decimal.`,`${base} × ${percent/100} es el aumento.`),explanation:`El aumento es $${base*percent/100}; se suma al valor inicial.`}}
export function generateEquation(x=6,m=3,b=4):Exercise{return{id:`eq-${x}-${m}-${b}`,type:"numeric",prompt:`Resuelve ${m}x + ${b} = ${m*x+b}`,answer:String(x),difficulty:4,hints:hints(`Resta ${b} en ambos lados.`,`Después divide entre ${m}.`,`${m}x = ${m*x}.`),explanation:`Al aislar x: (${m*x+b} − ${b}) ÷ ${m} = ${x}.`}}
