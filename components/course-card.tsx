import Link from "next/link";import type {Course} from "@/lib/types";import {ArrowRight} from "lucide-react";
const info={matematicas:{level:14,xp:2850,mastery:82},english:{level:6,xp:1240,mastery:71},finanzas:{level:4,xp:780,mastery:64},trading:{level:3,xp:520,mastery:58}};
export function CourseCard({course}:{course:Course}){const x=info[course.slug];return <Link className={`course-card ${course.color}`} href={`/courses/${course.slug}`} aria-label={`Continuar ${course.title}`}><div><span style={{opacity:.82,fontSize:12,fontWeight:800}}>ACADEMIA</span><h3>{course.title}</h3><p style={{opacity:.85}}>{course.subtitle}</p></div><div><div className="course-meta"><span>Nivel {x.level}</span><span>{x.xp.toLocaleString()} XP</span><span>{x.mastery}% dominio</span></div><div className="progress"><span style={{width:`${x.mastery}%`}}/></div><strong>Continuar <ArrowRight size={15} style={{display:"inline"}}/></strong></div></Link>}
