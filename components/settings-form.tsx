"use client";import {useState} from "react";import {useTheme} from "next-themes";
export function SettingsForm(){const{theme,setTheme}=useTheme();const[saved,setSaved]=useState(false);return <form className="card" style={{maxWidth:680}} onSubmit={e=>{e.preventDefault();setSaved(true)}}><h2>Experiencia y privacidad</h2><div className="field"><label>Idioma de interfaz</label><select defaultValue="es"><option value="es">Español</option><option value="en">English</option></select></div><div className="field"><label>Tema</label><select value={theme} onChange={e=>setTheme(e.target.value)}><option value="system">Sistema</option><option value="light">Claro</option><option value="dark">Oscuro</option></select></div><div className="field"><label>Objetivo diario (minutos)</label><input type="number" min="5" max="90" defaultValue="20"/></div>{[["Sonidos suaves",true],["Dificultad adaptativa",true],["Recordatorios dentro de la app",false]].map(([x,on])=><label key={x as string} style={{display:"flex",gap:10,margin:"17px 0"}}><input type="checkbox" defaultChecked={on as boolean}/>{x as string}</label>)}<div className="notice"><strong>Protección infantil</strong><p style={{margin:"5px 0 0"}}>No solicitamos ubicación, nombre completo ni perfil público. No existen mensajes de desconocidos, publicidad o rankings sociales.</p></div><button className="btn" style={{marginTop:18}}>Guardar preferencias</button>{saved&&<span style={{marginLeft:12,color:"var(--success)"}}>Preferencias guardadas</span>}</form>}
