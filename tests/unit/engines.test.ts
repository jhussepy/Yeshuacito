import {describe,expect,it} from "vitest";import {calculateXP,levelFromXP,starsForAccuracy} from "../../features/gamification/engine";import {adaptiveDifficulty,masteryBand,updateMastery} from "../../features/progress/mastery";import {calculateDrawdown,calculatePnL,calculatePositionSize,calculateRR,calculateRisk,processXP} from "../../features/trading/risk";import {generateAdditionQuestion,generateEquation,generateFractionComparison,generatePercentageProblem} from "../../features/math/generators";
describe("gamificación",()=>{it("limita bonus y reduce farming",()=>{expect(calculateXP({correct:3,total:3,difficulty:4,completed:true,streak:99})).toBe(160);expect(calculateXP({correct:3,total:3,difficulty:4,completed:true,streak:99,priorCompletions:9})).toBe(32)});it("calcula niveles y estrellas",()=>{expect(levelFromXP(900)).toBe(4);expect(starsForAccuracy(.91)).toBe(3)})});
describe("dominio adaptativo",()=>{it("respeta bandas y límites",()=>{expect(masteryBand(95)).toBe("Maestría");expect(updateMastery(99,true,5,4000)).toBe(100);expect(updateMastery(1,false,5,20000)).toBe(0)});it("refuerza tras errores",()=>expect(adaptiveDifficulty(88,2)).toBe(1))});
describe("riesgo",()=>{it("dimensiona desde pérdida máxima",()=>{expect(calculateRisk(10000,1)).toBe(100);expect(calculatePositionSize(10000,1,100,98)).toBe(50);expect(calculateRR(100,98,106)).toBe(3)});it("calcula pnl y drawdown",()=>{expect(calculatePnL("SELL",100,95,20)).toBe(100);expect(calculateDrawdown(10000,9000)).toBe(10)});it("premia proceso sobre resultado",()=>expect(processXP({hasStop:true,riskPercent:1,followedPlan:true,pnl:-50})).toBeGreaterThan(processXP({hasStop:false,riskPercent:4,followedPlan:false,pnl:100})))});
describe("generadores matemáticos",()=>{it("produce respuestas coherentes",()=>{expect(generateAdditionQuestion(1).answer).toBe("15");expect(generateFractionComparison().answer).toBe("2/3");expect(generatePercentageProblem().answer).toBe("108");expect(generateEquation().answer).toBe("6")})});
