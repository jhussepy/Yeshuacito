import {test,expect} from "@playwright/test";test("dashboard, course and lesson are navigable",async({page})=>{await page.goto("/");await expect(page.getByRole("heading",{name:/qué descubriremos/i})).toBeVisible();await page.getByLabel("Continuar Matemáticas").click();await expect(page.getByText("Mapa de aprendizaje").first()).toBeVisible();await page.getByRole("link",{name:/Sentido numérico/}).click();await expect(page.getByRole("button",{name:"Empezar práctica"})).toBeVisible()});
